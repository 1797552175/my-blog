#
# 说明：
# - 这个文件是“示例模板”，用于说明各环境变量用途（不要直接当生产密钥使用）
# - 复制为 config/env.local 后，本地可用 scripts/start-api.ps1 启动后端（自动读取 env.local）
# - 服务器部署建议在 `my-blog/deploy/.env` 单独放生产值（不要提交到 git）
#

# === 可选：JDK 路径（start-api.ps1 会读取，未设置则用系统 JAVA_HOME）===
JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.9.10-hotspot

# === DB（后端 Spring Boot 连接用） ===
# 数据库主机（本地开发可用 127.0.0.1；docker-compose 内建议用服务名 db）
DB_HOST=127.0.0.1
# 数据库端口：
# - 本地 docker DB：一般是 3307（宿主机映射到容器 3306）
# - SSH 隧道到服务器：你现在是 13306 -> server:3306
DB_PORT=3307
# 数据库名
DB_NAME=blog
# 数据库用户（本项目 compose 默认创建 blog 用户）
DB_USER=blog
# 数据库密码（本地 docker DB / 服务器 DB 都需要正确的密码）
DB_PASSWORD=abc123

# === DB（仅用于本地 docker 启 DB 时初始化 root 密码）===
# 注意：这个变量只对 `deploy/docker-compose.yml` 里 db 服务生效，后端连接不用它
DB_ROOT_PASSWORD=abc123

# === JWT（后端签发/校验 Token 用） ===
# 生产必须是“足够长的随机字符串”，避免被暴力破解
JWT_SECRET=please_change_me_please_change_me_please_change_me

# 可选：JWT 过期时间（毫秒），默认 86400000（24h）
# JWT_EXPIRATION=86400000

# === AI（首页灵感对话、作者分身）===
# 大模型 API 地址（默认 OpenAI；DeepSeek 等兼容接口可改）
# AI_API_URL=https://api.openai.com/v1/chat/completions
# 大模型 API Key（必填，不设则 AI 功能不可用）
# AI_API_KEY=sk-xxx
# 模型名。OpenAI 默认 gpt-4o-mini；DeepSeek 用 deepseek-chat 或 deepseek-reasoner（不要填 DeepSeek-V3.2）
# AI_MODEL=gpt-4o-mini
# AI_MODEL=deepseek-chat

# === Redis（分身对话上下文缓存）===
# 本地开发：docker compose up -d redis 后，用 127.0.0.1:6379
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379

# === CORS（跨域配置，可选）===
# 允许的前端域名/IP列表，多个地址用逗号分隔
# 生产环境建议只配置真实的前端地址
# 示例：CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,http://192.168.1.100:3000
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost
