#
# 说明：
# - 服务器部署时，将此文件复制为 .env：cp env.example .env
# - 然后修改 .env 中的密码和 JWT_SECRET 为生产环境值
# - .env 不要提交到 git（已在根目录 .gitignore 中排除）
#
# 用于：在 deploy 目录执行 docker compose up 时，Compose 会读取此目录下的 .env
#

# 数据库密码（db 服务初始化 + api 服务连接都用此密码）
DB_PASSWORD=your_strong_password

# MariaDB root 密码（仅 db 服务初始化用）
DB_ROOT_PASSWORD=your_strong_root_password

# JWT 密钥（api 签发/校验 Token 用，生产必须足够长且随机）
JWT_SECRET=a_very_long_and_secure_secret_key_for_production

# === AI（首页灵感对话、作者分身，可选）===
# 不配置则 AI 功能不可用。Redis 由 docker-compose 自动起，api 已配置连接。
# 大模型 API Key（必填）
AI_API_KEY=
# 可选：API 地址，默认 OpenAI
# AI_API_URL=https://api.openai.com/v1/chat/completions
# 可选：模型名，默认 gpt-4o-mini
# AI_MODEL=gpt-4o-mini
